<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:appNamespace="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="yosoyo.aaahearhereprototype.HolderActivity">
      <item android:id="@+id/post"
            android:title="Post"
            android:icon="@android:drawable/ic_menu_add"
            appNamespace:showAsAction="ifRoom"
          />
      <!--<item android:id="@+id/search"
            android:title="Search"
            android:icon="@android:drawable/ic_menu_search"
            appNamespace:showAsAction="collapseActionView|ifRoom"
            appNamespace:actionViewClass="android.support.v7.widget.SearchView" />-->
      <item android:id="@+id/action_reset_database"
            android:title="@string/action_reset_databse"
            android:orderInCategory="99"
            appNamespace:showAsAction="never"/>

      <item android:id="@+id/action_settings"
            android:title="@string/action_settings"
            android:orderInCategory="100"
            appNamespace:showAsAction="never"/>

</menu>
